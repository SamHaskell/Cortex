#include "Cortex/Graphics/RenderContext.hpp"

namespace Cortex
{
    RenderContext::RenderContext(const std::unique_ptr<RenderDevice> &renderDevice)
    {
    }
    RenderContext::~RenderContext()
    {
    }
}